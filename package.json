{
    "name": "rsvp-reader",
    "version": "1.0.0",
    "description": "Speed reading app with ORP highlighting",
    "main": "index.html",
    "scripts": {
        "start": "python3 -m http.server 8080",
        "build": "mkdir -p www/lib/parsers && cp index.html index.css app.js manifest.json sw.js www/ && cp -r lib/* www/lib/",
        "cap:add": "npm run build && npx cap add android",
        "cap:sync": "npm run build && npx cap sync",
        "cap:open": "npx cap open android"
    },
    "keywords": [
        "rsvp",
        "speed-reading",
        "spritz",
        "orp"
    ],
    "author": "",
    "license": "MIT",
    "devDependencies": {
        "@capacitor/cli": "^6.0.0",
        "typescript": "^5.9.3"
    },
    "dependencies": {
        "@capacitor/android": "^6.0.0",
        "@capacitor/core": "^6.0.0"
    }
}
